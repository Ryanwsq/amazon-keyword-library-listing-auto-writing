# Amazon Keyword Library

- Project name: Amazon产品关键词知识库
- Project slug: `amazon-keyword-library`
- Purpose: 从真实产品输入开始，完成关键词来源采集、品类清洗、分类、词频、竞争、趋势、最终装配和独立验收。
- Primary users: Amazon运营、Listing和广告团队
- Repository sharing classification: sanitized
- Last reviewed: 2026-08-31

## Current state

- 独立私有仓库：`Ryanwsq/amazon-keyword-library`；`main`是唯一长期基线，当前合同更新在短期功能分支完成。
- 当前长期主任务：`Amazon关键词词库｜主任务｜main`；十个长期单一职责副任务已在本机建立，实际任务ID、路径和授权只保存在Git忽略映射。
- 当前知识基线仍是清洗V2.1；V2.2只标识词频组件。2026-08-21的来源减配、输出合同和装配规则属于Post-V2.1增量，不命名为新清洗版本。
- 当前共十二个Skills：十个业务Skill和两个维护Skill。全部保持`draft`，全部能力保持`planned`；本轮文档、合同和P0验证不构成P1。
- 十二个Skill均已建立`evidence/index.md`，每个固定登记两个正常案例和一个边界/异常案例。2026-08-27首批模块发布后，36个槽位为11个`accepted`、1个`candidate`、24个`planned`；6个正常案例和5个边界案例已接纳，publication正常案例候选待用户复核。索引与单个案例不等于P1。
- 首轮真实正常案例已经跑到旧结构的最终工作簿和独立QA，但Amazon联想为`not_executed`，且结果生成在本次输出合同锁定之前，只能作为问题发现证据。
- 开头输入固定为三组：`产品基础信息配置`、`产品配置卖点`、`竞品对标ASIN`。产品基础信息必须已经包含目标Amazon类目及是否存在多个稳定产品类型细分；字段齐全后运行途中不得重复询问，缺失则Run不启动。用户不指定核心词；主任务依据锁定事实卡、直接竞品身份和SIF候选独立确认唯一一级品类核心大词，仅在类目有多个稳定产品类型细分时确认可选唯一细分核心词。单独卖点、配置、场景或高流量长词不得建立细分核心层级。
- 核心层级继续决定主执行锚点与来源锚点：有细分核心词时主执行锚点和Amazon联想锚点使用该词，卖家精灵分别使用一级核心词和细分核心词；无细分核心词时三者都只使用一级核心词。SIF覆盖、返回位置、ABA和搜索量不能跨语义层级晋级。
- 第二板块Sheet2新增`通用词库资格`并由13列调整为14列；品类相关不再自动等同为产品强相关。2026-08-24进一步拆分两级歧义：一级品类归属无法确认时进入Sheet3人工复核；仅当一级品类归属已确认、目标细分关系仍不明时才留在Sheet2并写`关系待复核/待复核`。分类只传递资格，词频、竞争和趋势只处理资格为`纳入`的适用人口，最终通用词库只从品类相关且资格纳入行机械筛选；最终总表调整为51+N。以上为Post-V2.1合同修正，仍非P1。
- 2026-08-24已把分类完成的Sheet4二类词人口增加为最终工作簿独立`二类词`Sheet；最终工作簿由七Sheet调整为八Sheet。该Sheet固定复制Sheet4原十二列及分类追加四列，不重新判断、允许零行，也不改变二类词的清洗、流量分类或下游分析边界。
- 分类状态精确使用`关键词ABA排名缺失、搜索量缺失、没有搜索量`；受影响派生字段不伪造，仍可形成可装配候选。2026-08-31进一步确认：准确传递计数、主键、原值和受影响派生字段后，这三种状态是QA允许缺口，可记`pass/completed_with_gaps`，不再等待运行途中人工确认。最终`品类产品通用词库`仅把F1–F5五个流量块表头显示为`F1 核心大词、F2 二级词、F3 中流量词、F4 中长尾词、F5 长尾词`，内部编码和阈值不变。
- 2026-08-22已锁定词频介词白名单`EN_PREP_CORE_V1`：48个精确token从单词词频排除并作为双词硬断点；指定冠词、连词和多义词保留，不做上下文词性或多词介词解析。原blocked证据保留；当前只形成有界机械验证，不构成完整来源或P1。
- 来源入口已确认：Amazon联想有细分核心词时使用该词，否则使用一级核心词；首选内置浏览器、普通Chrome备用，只采上方关键词联想区，底部商品卡、价格卡和商品轮播排除。SIF首选MCP、同提供商已登录网页备用。卖家精灵关键词挖掘首选其拥有副任务的已登录官网完整官方导出，从导出记录取得完成文件；官网不可用时才使用同提供商MCP。有细分核心词时一级/细分两个种子分别导出并在模块内机械去重，无细分时只用一级核心词。入口切换保持同查询/周期/过滤/字段和证据门。趋势固定`SellerSprite -> Sorftime`，同一Run单一提供商；月/季图展示实际搜索量，环同比只留表格。
- 此前真实全流程验证因使用备用来源、运行中规则纠正和人工覆盖，只能作为边界/异常候选；它曾按当时七Sheet合同通过全部21项适用Gate并得到`completed_with_gaps`，但现在只作为旧合同证据，不能证明当前八Sheet合同完成或通过。
- 正式运行固定为一次用户输入后由主任务调度至最终交付；运行期间知识、Skills和流程只读，问题统一进入一个问题文档，整轮结束后再修改规则。
- 执行模块强制由对应长期副任务完成并正式回传；每次派发前机械核对固定任务身份、host、cwd、Run、revision和输出目录，主任务不得代跑或使用临时替代任务。只有用户明确的一次性Run例外可接收已经产生的证据，且不能成为默认流程或P1证据。
- 2026-08-25已确认第二板块配置假阴性和同类目二类词修正：普通材质/结构/功能/配置不因目标SKU不具备而降级，也不要求词面显式包含细分核心词；用户确认类目有多个稳定产品类型时，同类目另一稳定直接替代产品可进入Sheet4。清洗和QA都必须反查`不纳入`、Sheet3和Sheet4假阴性。
- 2026-08-25已确认趋势包级审计、隐私扫描和QA后封包修复：遍历全部worksheet/drawing/chart，真实任务ID/本机路径失败而SHA/Office GUID通过；最终封包采用候选装配、不可变最小QA产物、装配重哈希和QA只读差异核对四阶段。
- 2026-08-26已确认多细分类目的目标细分简称/紧凑表达强等价闭环：省略一级品类或用途等上位限定词但仍保留决定性细分身份和完整商品头部时，必须结合产品事实、直接竞品同对象身份和SIF证据逐项判断；不得因缺少这些token机械写成宽泛/相邻、摘除、不纳入或二类词。清洗发现锚点冲突时停止受影响输出，QA按行审计而不由共享理由模板外推整组。
- 2026-08-31已确认质量双模式：日常规则/Schema/检查器均未变化的真实Run默认`compact-production`，仍执行21项机械全量检查和完整风险人口逐行语义复核，复用装配渲染，只生成compact结果与按需唯一问题文档；规则/Skill/判断边界、字段/Sheet/Schema、公式/图表、封包/检查器变化、P1三案例或compact异常必须使用`full-regression`。精简只删除重复质量工作簿/预览，不删除任何业务边界或Gate。
- 最新锁定Run已从三个来源完成到八Sheet业务工作簿，用户于2026-08-27人工检查并确认业务内容无问题。用户同时明确跳过本轮独立QA和QA后封包，因此业务工作簿已人工接纳，但正式交付、21项门和端到端P1仍为`false`；部分QA尝试与残缺封包只保留为本机问题证据。

## Confirmed execution graph

1. 主任务从三组开头输入锁定产品事实、产品基础信息中已给出的目标Amazon类目及是否存在多个稳定产品类型细分、竞品、站点、Run和规则版本；字段齐全后不再重复询问。
2. SIF竞品反查副任务先运行；主任务依据事实卡、直接竞品身份和SIF候选独立确认一级品类核心大词，仅在类目有多个稳定产品类型细分时确认可选细分核心词并完成目标细分简称/紧凑表达强等价闭环，再锁定主执行锚点、Amazon联想锚点和卖家精灵一至两个种子。
3. Amazon联想与卖家精灵扩词并行。
4. 主任务机械合并三来源，输出第一板块两Sheet工作簿。
5. 第二板块完成Sheet2/3/4三去向及Sheet2通用词库资格清洗。
6. 清洗闭环后，资格纳入人口的词频与完整Sheet2/Sheet4关键词分类并行。
7. 分类完成后，资格纳入人口的竞争性与趋势性并行。
8. 最终装配生成过程文件夹和八Sheet最终工作簿，其中`二类词`独立Sheet机械复制分类完成的Sheet4人口。
9. 独立质量验证按锁定模式只读执行全部21项门和完整风险人口审计；日常默认compact，触发回归条件时使用full，未通过不得标记完成。

## Module status

| 模块 | 当前合同 | 当前状态 |
|---|---|---|
| SIF竞品反查 | 七列业务明细；SIF MCP首选、同提供商网页备用 | 已接纳一个官网备用边界案例；两个正常案例待执行，draft/planned |
| Amazon联想 | 必选；有细分核心词时使用细分核心词，否则一级核心词；内置浏览器首选、普通Chrome备用；Amazon US未登录、All、10001、完整固定矩阵；排除底部商品/价格卡和商品轮播 | 已接纳正常案例01；正常案例02与边界案例待执行，draft/planned |
| 卖家精灵扩词 | 有细分核心词时一级/细分双种子，否则单一级种子；拥有副任务已登录官网完整官方导出优先、同提供商MCP备用；逐种子原始导出和Pass、模块内机械去重四列业务表 | 已接纳单种子官网导出正常案例01；其余两案例待执行，draft/planned |
| 三来源机械合并 | 两Sheet；分层锚点、目标细分强等价闭环、联想锚点、宽泛/相邻流量词与一至两个卖家精灵种子分别记录 | 本轮机械合并闭合；总控因最终QA取消登记边界案例，两个正常案例待执行 |
| 品类清洗 | 四Sheet；Sheet2固定14列并闭合通用词库资格；同类目稳定直接替代可为二类词；配置、细分词字面和上位限定词缺失不作负面硬门 | 已接纳正常案例01；其余两案例待执行，draft/planned |
| 分类 | 完整传递Sheet2十四列和资格，再追加4固定列+N动态语义列；Sheet4追加4固定列；最小否词库；ABA/搜索量缺口精确标记并作为QA允许缺口自动闭合 | 已接纳正常案例01；其余两案例待执行，draft/planned |
| 词频 | 只读资格纳入的Sheet2英文词；`EN_PREP_CORE_V1`固定48-token排除与双词断点 | 已接纳独立加号最小修正边界案例；两个正常案例待执行，draft/planned |
| 竞争 | 资格纳入的Sheet2 F1–F4；只用SIF Top3点击/转化份额；12列 | 已接纳正常案例01；其余两案例待执行，draft/planned |
| 趋势 | 资格纳入的Sheet2 F1–F3；`SellerSprite -> Sorftime`且单Run单源；至少24完整月；月/季实际搜索量图；全worksheet/drawing/chart包级审计 | 已接纳SellerSprite正常案例01；其余两案例待执行，draft/planned |
| 最终装配 | 两个顶层对象；最终工作簿固定八Sheet；总表覆盖三去向51+N列；二类词独立Sheet机械复制分类Sheet4；按qa_mode封包、隐私扫描与QA后最终重哈希 | 已接纳“业务候选通过、用户取消QA后seal”的边界案例；两个正常案例待执行 |
| 独立质量验证 | compact/full双模式；两种模式均保留21项门与完整风险人口逐行审计；compact复用装配渲染和JSON结果，full保留完整质量工作簿/manifest/必要预览；最终封包后只读差异核对 | 已接纳“用户取消时不输出pass”的边界案例；两个正常案例待执行 |

## Final delivery contract

顶层只交付：

1. `过程性文件/`：分第一板块、第二板块、第三板块和独立质量验证四个目录，并含唯一`process-manifest.json`。
2. `<Run_ID>-最终关键词词库.xlsx`：仅八个可见Sheet，顺序固定为`最终关键词决策总表`、`SKU事实卡`、`品类产品通用词库`、`二类词`、`关键词竞争性分析`、`关键词趋势性分析`、`词频统计`、`否词库`。

最终总表覆盖第一板块机械去重全人口，每个`Keyword_ID`恰好一行并标记`品类相关/其他摘除/二类词`，固定51+N并传递`通用词库资格`。第二板块二类词继续保留在过程工作簿Sheet4并全量进入最终总表；最终独立`二类词`Sheet机械复制分类完成的Sheet4全人口和固定16列，允许只有表头，不在装配阶段重新判断。详细Top3及竞争结构只在独立竞争Sheet；广告资格和投放动作当前统一为`未评估/后置`。

封包顺序固定为候选装配、独立QA一次性模式产物、装配后最终重哈希、QA只读封包差异核对。compact质量目录只含结果JSON及按需一个问题文档/引用；full保留质量工作簿、quality manifest、必要独立预览和一个问题文档/引用。最终`process-manifest.json`不计算自身，质量目录按模式使用允许清单，未列文件零容忍。交付隐私扫描覆盖文本、路径、XLSX单元格与OOXML结构，真实Codex任务ID/本机路径必须失败，普通SHA-256与Office内部GUID不得误报。

## Open work

- 用户复核本批跟踪变更后，决定publication Skill的`case-01-normal`候选是否改为`accepted`。
- 确认动态语义列精简后Sheet4 F5唯一主分组的取值依据。历史合同曾在不输出完整语义多标签列的前提下，内部沿用固定语义维度产生Sheet4 F5主标签；精简改为Sheet2动态语义列后，Sheet4仍不输出动态列，但保留了`动态列名:标签值`要求，当前内部依据尚未确认。F5主标签选择优先级本次不处理。
- 本批修改质量合同和封包检查器，下一次验证必须使用`full-regression`；回归锁定后，未发生规则/结构/检查器变化的日常真实Run默认使用`compact-production`。已有正常案例的Skill优先补正常案例02与边界案例；当前只有边界案例的总控、SIF、词频、最终装配和独立QA仍需两个正常案例；不得用本轮人工确认倒填未执行的QA或seal。
- 完成两个正常案例和一个边界/异常案例的真实P1；此前不得提升Skill成熟度。
- 广告否定方式、广告资格和投放动作仍是后置模块；当前否词库仅保留语义否词候选，不输出精准/词组否定类型。

## Success criteria

- 三来源尽可能完整抓取；局部字段或接口异常不吞掉已经取得的关键词。
- 第一板块机械词池、第二板块三去向和最终总表人口逐层闭合，主键、来源、周期和版本可追溯。
- 分类可从锁定上游复算；词频、竞争、趋势和通用词库均可从`通用词库资格=纳入`的锁定人口机械复算。
- 最终只有两个顶层交付对象，最终工作簿恰好八个可见Sheet；`二类词`Sheet与分类Sheet4人口、主键、字段和值机械一致。
- 独立QA无论compact或full都须通过全部适用硬门和完整风险人口审计，才可标记`completed`或`completed_with_gaps`。

## Source of truth map

- 人类可读端到端流程：`docs/end-to-end-workflow.md`
- 稳定领域知识：`knowledge/product-keyword-library.md`
- 历史案例证据：`knowledge/keyword-cleaning-case-evidence.md`
- 已确认版本决策：`knowledge/keyword-decision-log.md`
- 当前完成度和下一步：本文
- 判定门槛与人工升级：`docs/keyword-judgment-boundaries.md`
- 可重复执行流程：`.agents/skills/<single-purpose>/`

## Boundaries

- 不保存账号、Token、原始接口响应、真实业务工作簿、任务ID、绝对路径或未脱敏数据到Git或飞书发布正文。
- 修改任何项目知识或Skill时，同批同步`docs/end-to-end-workflow.md`；未同步不得验收。
- 正式只读运行中不改规则；问题先集中记录，运行结束并经用户确认后才进入规则迭代。
