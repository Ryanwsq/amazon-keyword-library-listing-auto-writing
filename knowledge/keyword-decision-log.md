# Amazon 关键词库版本决策记录

- Status: verified
- Last verified: 2026-08-27
- Sharing: sanitized
- Provenance: 用户授权归档、用户确认记录、四轮历史案例脱敏结论、2026-08-11至2026-08-16增量决策、2026-08-20首轮真实正常案例问题台账、2026-08-21首轮运行后减配确认、2026-08-22词频介词白名单确认、2026-08-23核心层级与通用词库资格确认、2026-08-24二类词独立Sheet/输入核心门/网页导出优先/任务分离/联想边界/两级歧义/分类数据缺口与显示名确认、2026-08-25来源双种子/同类目二类词/配置假阴性/机械任务身份/审计与QA后封包确认、2026-08-26多细分类目目标细分简称强等价闭环确认，以及2026-08-27十二Skill证据登记治理确认
- Coverage: confirmed decisions through V2.1 plus V2.2 word frequency, 2026-08-21 output contracts, 2026-08-22 clarifications, 2026-08-23 core hierarchy/general-library eligibility correction, 2026-08-24 output and input gates, 2026-08-25 conditional dual-seed collection/same-category secondary terms/configuration-safe eligibility/task identity/trend audit/privacy and post-QA packaging, 2026-08-26 multi-subdivision target-subdivision alias closure, and 2026-08-27 evidence-registry governance

## 摘要

本文件只记录已确认版本决策、替代关系和理由。稳定领域定义由 `product-keyword-library.md` 拥有，当前进度和下一步由 `../PROJECT.md` 拥有，判定门槛和执行流程不在本文件重复维护。

## 版本记录

| 版本 | 日期 | 变更 | 状态 |
|---|---|---|---|
| V0.1 | 2026-08-06 | 建立三板块框架、核心对象、共用原则、完成标准和逐板块更新机制 | superseded |
| V0.2 | 2026-08-06 | 建立 SIF 竞品反查、核心大词判断、卖家精灵扩词、原始词池和质检门 | superseded |
| V0.3 | 2026-08-06 | 明确 SIF 校准方向并辅助确认核心大词，卖家精灵负责大范围扩词 | superseded |
| V0.4 | 2026-08-06 | 明确三类用户输入；先由竞品 SIF 流量词发现品类候选，再由产品信息和配置核对 | superseded |
| V0.5 | 2026-08-06 | 确认核心大词判断；流量大小使用 ABA 或月搜索量，并与 SIF 竞品流量份额分离 | superseded |
| V0.6 | 2026-08-06 | 加入 Chrome 无痕搜索框联想：基础、前后 A–Z、连接词、配置和条件性数字；排除问题式联想 | superseded |
| V0.7 | 2026-08-06 | 曾确认 SIF 30 天并追加 7 天、每 ASIN 300 条、多竞品优先、1–3 个种子和 MCP 固定错误提示 | superseded by V0.8 |
| V0.8 | 2026-08-06 | 修正为通常 3–5 个竞品、SIF 只查 30 天、ABA 优先、近义种子去重和正常零结果重拉 | superseded by V0.9 |
| V0.9 | 2026-08-06 | 搜索框只采集主核心大词；品类功能与关键配置无论 SKU 是否具备都可试探并记录适配状态 | confirmed, incorporated |
| V1.0 | 2026-08-06 | 固定第一板块十表 Excel、来源明细、机械匹配键、跨表主键和交付质检门 | confirmed, incorporated |
| V1.1 | 2026-08-06 | 启动清洗板块，早期口径为不依据目标 SKU 信息、只从有效池排除无关词 | superseded by V1.2 |
| V1.2 | 2026-08-07 | 建立二类词库，取消精准/关联/补充分流；第二板块不新增词，二类和无关词不参加主词频 | superseded by V1.3 |
| V1.3 | 2026-08-07 | 升级为跨类目中心购买对象框架；品牌、Gift、配件/互补品、泛词/场景进入 Sheet3，直接替代商品进入 Sheet4 | superseded by V1.4 |
| V1.4 | 2026-08-07 | 流量选区改为并列 OR：ABA 排名 100 万前，或月搜索量大于 100 | confirmed, incorporated |
| V1.5 | 2026-08-07 | 确认 Sheet3 否词准备和 Sheet4 二类扩量的下游接口及误伤边界 | confirmed, incorporated |
| V1.6 | 2026-08-07 | 完成 Gaming Chair 第二轮初次验证并提出相邻完整商品、多商品组合、多语言和商品头部边界 | superseded by V1.7 |
| V1.7 | 2026-08-07 | 用户确认并修正 Gaming Chair 边界与最终数量，补充品牌系列型号和符号标准化 | confirmed, incorporated |
| V1.8 | 2026-08-07 | 完成 Vanity 第三轮初次验证，并把正式方法重构为六层跨类目语义判断 | superseded by V1.9 |
| V1.9 | 2026-08-07 | 收紧二类词为相同主要购买目的和直接替代；来源只发现候选，Sheet4 允许为零；修正 Vanity 结果 | confirmed, incorporated |
| V2.0 | 2026-08-07 | 完成 Office Chair 第四轮验证，补充同品类层级、方向性替代、主要任务、宽泛歧义和配置/配件语法中心 | superseded by V2.1 |
| V2.1 | 2026-08-07 | 拆分一级品类核心大词、产品细分品类词和 SKU 精准适配；Sheet2 定义为清洗后品类相关关键词库 | current, user-confirmed |
| Post-V2.1 | 2026-08-20 | 首轮正常案例规则回写；不改变V2.1清洗版本，旧工作簿待重跑 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-21 | 首轮运行后减配卖家精灵/SIF字段、主副任务回传和Top3竞争分析；不改变V2.1清洗版本 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-21 | 锁定两对象交付、过程输出、三去向50+N总表、七Sheet最终工作簿、介词过滤词频、24月趋势和21项QA门 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-22 | 澄清主执行锚点、已确认核心词与完整词覆盖优先级；不改变V2.1三去向定义 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-23 | 修正核心层级、卖家精灵单一种子和通用词库资格；三去向不变，Sheet2与下游人口合同更新 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-24 | 最终工作簿新增独立二类词Sheet，由七Sheet调整为八Sheet；二类词定义、三去向和分析范围不变 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-24 | 新增用户类目细分输入门、卖家精灵网页完整导出优先和严格长期副任务执行边界 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-24 | 明确Amazon联想固定使用一级品类核心大词，并排除底部商品卡、价格卡和商品轮播 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-24 | 拆分第二板块两级歧义：一级品类归属不明进Sheet3，仅目标细分关系不明才在Sheet2待复核 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-24 | 锁定ABA/搜索量三种精确缺口状态，将其从分类整批停止门后置到最终QA用户确认；锁定通用词库五个流量块显示名 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-25 | 修正来源锚点、同类目二类词与配置假阴性，增加任务身份门、趋势审计、隐私扫描和QA后封包闭环；本轮不重跑 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-26 | 多细分类目增加目标细分简称/紧凑表达的强等价闭环、层级来源和冲突停止门；当前受影响Run获准按新规则修复 | confirmed increment; P1 pending |
| Post-V2.1 | 2026-08-27 | 十二个Skill建立三槽位证据登记，锁定真实案例接纳、禁止历史倒填和模块独立证据边界；业务流程不变 | confirmed governance increment; P1 pending |

## V2.1 决策说明

V2.1 正式拆分一级品类核心大词、产品细分品类词和 SKU 精准适配，纠正把“同品类但配置不同”在第二板块提前删除的风险。第二板块回答完整关键词是否仍在一级品类内。2026-08-11 后续结构决策取消了第三板块 SKU 事实匹配；这一取消不改变 V2.1 的清洗边界。

Office Chair 历史案例暴露了类目锚点与 SKU 精准适配混淆，并推动这一修正。该案例只解释决策来源，不应被复制成其他类目的词级规则。

## 2026-08-11：V2.1 后第三板块流程结构决策

本次是对后续流程结构的增量确认，不改变 V2.1 的清洗规则。2026-08-11 用户进一步修正竞争、趋势和最终工作簿输出，并授权建立或更新相应 draft Skills；这不等于第三板块已形成经验证执行版本。已确认：

1. 第三板块包括关键词分类、核心词竞争性分析、Sheet3 否词库设计、Sheet4 二类词流量分类和品类级趋势性分析，之后装配最终工作簿。
2. Sheet2 先按 ABA 排名分为 F1–F5，再做词性/词义多标签；F5 每 20 个词形成 `LT-1`、`LT-2` 等长尾组。第三板块不读取 SKU 事实卡、不再做 SKU 事实匹配，也不生成相关状态字段。
3. F2 的正式名称为“二级流量词”，与 Sheet4 的“二类词”严格分开。
4. Sheet2 建立一词一行的核心关键词分类过程表；Sheet4 按同一流量层规则建立独立二类词流量分类 Sheet，只保留流量分层、LT 分组和替代关系，不做竞争性或趋势性分析；Sheet3 进入精准否定、词组否定、不否定和人工确认的否词库设计。
5. 竞争性只保留六项判断对象：SIF Top3 ASIN点击份额、SIF Top3 ASIN转化份额，以及第一板块已采集的卖家精灵 SPR、CPC、商品数量和点击集中度。第三板块不得重新调用卖家精灵关键词挖掘或按精确词补拉；缺失字段回到第一板块处理，需要更新周期时重跑版本化的第一板块采集批次。SIF 只使用关键词竞争格局与关键词机会筛选两个接口，不使用词根维度竞争者识别；机会筛选的其他返回字段不进入竞争性判断。六项指标在核心关键词中分别形成独立数值列，另保留一个`关键词竞争性分析`综合字段；阈值、冲突和缺失值规则仍待确认。
6. 趋势性只分析 Sheet2 中 F1–F4 的搜索量、ABA 和精确词/长尾需求结构，使用卖家精灵 ABA 月度趋势与 SIF 词根需求趋势；不使用西柚 MCP，也不使用购买量、购买率或需求质量。一个品类统一输出一个趋势性分析 Sheet，包含品类摘要和 F1–F4 逐关键词月度明细；F5、Sheet3、Sheet4 不进入，趋势也不写回最终关键词决策总表。趋势分类阈值未确认时综合趋势写`待确认`。
7. 最终工作簿的第一张`Sheet1_最终关键词决策总表`只收录 Sheet2 核心关键词，一词一行；Sheet3 否词库和 Sheet4 二类词不得混入。多来源在`来源渠道`汇总，逐条来源仍留在过程 Sheet。
8. Sheet1 至少保留身份、翻译、来源、ABA、搜索量、SPR、CPC、流量层、LT 分组、词性/词义多标签、Top3 ASIN点击份额、Top3 ASIN转化份额、商品数量、卖家精灵点击集中度、竞争综合字段、完整性、复核和版本字段。
9. 第一、第二、第三板块实际生成的全部过程 Sheet 必须保留；Sheet4 二类词、Sheet3 否词库和 F1–F4 趋势分别独立成 Sheet。最低清单不构成删除额外过程 Sheet 的许可。
10. 广告资格、用户后续提供的通用广告框架，以及只用一张 Sheet 输出的品类通用词库，均后置并保持待定。
11. 另一台电脑已完成的词频结果只作为后续分类的版本化本地输入；其算法和结果当时尚未同步到本仓库，本次不重算、不创建词频 Skill。此临时状态已被 2026-08-16 的 V2.2 词频组件迁入决策替代。

在 2026-08-11 当时，ABA 异常值处理、标签冲突、否词误伤边界、竞争阈值与冲突规则、品类趋势方向一致性、趋势方向/季节性/尖峰阈值和行动建议均未确认；其中竞争性部分由下方 2026-08-13 决策替代，其余仍保持未确认。

## 2026-08-13：竞争性分析来源、独立 Sheet 与判断边界

本次确认替代 2026-08-11“六项竞争指标直接铺入核心词表、竞争阈值待确认”的口径，不改变 V2.1 清洗基线，也不改变第三板块不读取 SKU 事实卡、广告资格后置和趋势独立 Sheet 的决策。已确认：

1. 竞争性分析只覆盖 Sheet2 的 F1–F4；F5、Sheet3 和 Sheet4 不进入。
2. 竞争性字段较多，独立形成`第三板块_关键词竞争性分析`Sheet，一词一行。最终 Sheet1 不再保存全部竞争明细，只回写 `Competition_Record_ID`、综合竞争等级、竞争判断摘要、竞争数据完整性和人工复核状态。
3. 正式判断使用 SIF 的 CPC、Top3 点击份额、Top3 转化份额和市场 CVR；卖家精灵只提供第一板块已采集的 SPR 与商品数量。卖家精灵 CPC、点击集中度和转化集中度仅可作为原始过程证据保留，不进入正式综合等级。
4. SIF 只调用 `market_screen_keyword_opportunities` 和 `market_get_keyword_competition`；不调用 `market_get_keyword_root_competitors`。机会筛选结果必须精确匹配完整关键词；关键词竞争格局只补充集中度、历史分化、Top3 之外购买空间、头部流量结构和可进入性解释，不引入目标 ASIN位置或广告动作。
5. SIF 入场信号作为项目综合等级的一致性复核，不重复计分。相差两级或以上且无法解释时，停止正式输出并进入人工复核。
6. Top3 点击与转化使用固定绝对阈值：小于 30% 为低，30%–小于 50% 为中，50%–小于 70% 为高，大于等于 70% 为极高；两者相差至少 15 个百分点时标记结构分化方向。
7. CPC、SPR、商品数量和市场 CVR 使用同站点、同品类、同周期、同流量层的 P33/P67 相对分位；层内不足 10 个有效词时依次回退到 F1+F2 或 F3+F4、全部 F1–F4，仍不足 10 个时标记样本不足。
8. 综合等级按极高、高、中、低有序判断，四个竞争维度为头部锁定、SPR、CPC 和商品数量；市场 CVR只作最多一级修正，不可用高 CVR掩盖高头部锁定或 SPR+CPC 双高。
9. 独立竞争 Sheet 同时记录机会筛选精确匹配状态、各来源周期、比较池范围、有效样本数、分位回退层级和置信度，使综合等级能够复算；SIF 入场信号保留原值，仅在方向可明确归一时作一致性复核。
10. Top3 点击或转化缺失、CPC/SPR/商品数量缺失两项以上、机会筛选未精确匹配、周期方向冲突、入场信号与项目等级相差两级以上未解决，或关键词不属于 F1–F4 时，不输出正式综合等级。F5 在最终总表标记`不适用（F5）/范围外`，不能误报为数据缺失。
11. 竞争性分析不生成广告资格、直接打、测试、暂缓或广告结构结论；这些仍在最终关键词决策总表之后另行确认。

## 2026-08-13：建立关键词分类 draft Skill

用户确认基于知识库中已经确定的第三板块流程结构建立项目级关键词分类 Skill。该决定不补充新的业务阈值，也不改变 V2.1 清洗去向。已确认：

1. 新 Skill 名称为 `amazon-keyword-classification`，只负责第三板块分类，不承担清洗、词频、竞争/趋势、最终工作簿或广告资格。
2. Sheet2 可执行 F1–F5互斥流量层、F5每20词的 LT分组，以及关键词自身的基础词性/词义多标签；不读取 SKU 事实卡。
3. Sheet4 使用同一流量分层生成独立二类词流量表，不改变直接替代身份，不做竞争或趋势。
4. Sheet3 只建立否词候选设计结构；精准否定、词组否定、不否定的最终边界未确认时，不自动形成可投放结论。
5. ABA缺失/无效/不可比、F5并列跨组、类目专属标签、标签冲突和否词方式均作为人工停止门，不由 Skill 猜测补齐。
6. Skill 保持 `draft`，能力保持 `planned`；必须完成两个正常案例和一个边界/异常案例的真实运行后才可申请 `verified`。

## 2026-08-14：简化品类关键词趋势输出

用户判断同一品类内关键词的趋势主要跟随类目，因此将趋势性从逐词综合分析简化为品类级可视化观察。本决定替代 2026-08-11 的 F1–F4 趋势明细、近三月/同比/ABA/季节性/长尾结构和综合趋势口径，不改变 F1–F4 竞争性范围。已确认：

1. 趋势只覆盖 Sheet2 的 F1–F3；F4、F5、Sheet3 和 Sheet4 不进入趋势输出。
2. 每个品类只输出一个趋势性分析 Sheet，在其中放置一张历年、按月的搜索量折线图。
3. 折线图每条线代表一个关键词；横轴为年-月，纵轴为月度搜索量，图例为关键词。
4. 同表保留绘图数据矩阵和来源/周期/缺失信息，使图表可复算。缺失月份留空，不填 0、不插值、不用另一来源补齐。
5. 不再计算或输出近三月变化、同比、ABA 走势、峰值月、季节性、精确词/长尾结构、品类汇总搜索量线或综合趋势标签。
6. 正式折线图的月度搜索量来源固定为卖家精灵 `aba_research_trend`；SIF 趋势接口不再作为本板块待选或回退来源，不能与卖家精灵混用或补值。卖家精灵趋势接口不可用时标记`not executed`。
7. 趋势图继续作为独立 Sheet 保留在最终工作簿中，不写回 Sheet1，不直接生成广告资格或投放结论。

## 2026-08-16：迁入 V2.2 词频组件

用户提供并确认把另一台电脑导出的脱敏词频 Skill 与知识包补入本项目。`V2.2`在此只标识词频组件业务基线，不将整个项目清洗基线从 V2.1 自动升级为 V2.2。已确认：

1. 词频是第二板块品类清洗闭环之后的独立机械步骤；只读取 Sheet2 非空完整关键词，不读取 Sheet3、Sheet4、流量入口失败词、原始未清洗关键词或额外词表。
2. 统计副本执行 Unicode NFKC 兼容规范化和英文小写化，以空白、标点和连字符拆分；保留字母、数字、停用词、介词和可识别非英语词面，不合并单复数、近义词、拼写变体、词干、词序或跨语言表达。
3. 单词按实际出现次数累计；双词只统计同一关键词内相邻且保持原顺序的两个词面。两表按次数降序、同次规范化词面字符升序排列，保留频次为 1 的所有词面，不建立高/中/低分档或加权分数。
4. 结果写入工作簿副本最后的 `Sheet5_词频统计`；原工作簿和全部原 Sheet 不改变。同表保存单词/双词排名、词面、重复次数、来源与口径，以及输入、总次数和唯一词面数量闭环。
5. 词频只为 Listing 标题、Item Highlights 和五点描述提供词语组件、常见相邻组合与表达顺序参考；不得解释为搜索量、ABA、流量、点击、转化、竞争、趋势、算法权重、完整关键词相关性或 SKU 精准度，也不直接生成 Listing。
6. 新增项目 Skill `amazon-keyword-word-frequency` 和能力 `keyword.library.word-frequency`。Skill 保持 `draft`、能力保持 `planned`；迁入前的本地试算不反向充当 P1 evidence。
7. 清洗闭环后词频与关键词分类可以并行；分类不等待词频结果，也不用词频值做流量分层、语义标签、竞争或趋势判断。最终装配锁定词频版本用于血缘并保留实际生成的词频 Sheet；装配阶段不自行重算。

## 2026-08-20：首轮正常案例问题台账规则回写

本次把用户在首轮真实正常案例检查中逐项确认的处理规则写回稳定知识、判定边界、draft Skills 和端到端流程。案例仍因必选 Amazon 联想技术项为`not_executed`而未完成，不构成 P1 正常案例通过；旧工作簿是在规则修正前生成，必须重跑后才能代表新规则。已确认：

1. Amazon 联想是第一板块必选来源，固定使用 Codex 内置浏览器、Amazon US 未登录、Department=`All`、邮编=`10001`；只采集可见下拉建议，不按 Enter、不用搜索结果页、外部浏览器、网页搜索或 API 替代。当前“浏览器可打开但不能识别/操作”只作为待解决技术项，来源写`not_executed`，正常案例保持未完成而不改称边界案例。
2. SIF 调用前建立本机忽略目录，每次返回后立即保存完整原始响应和参数；最近 30 天未返回起始日时记录窗口参数、数据截止日和`接口未返回`。同一查询的每日变化按独立批次保存，不覆盖。
3. 卖家精灵声明总数漂移但页数稳定、分页连续且存在明确最后页时可继续，实际返回行数为行数真值；边界重复原始行保留，总表按机械键合一条。来源完成与逐行指标完整分开，缺字段留空、不补 0、不按精确词重拉。
4. 规范 ABA/搜索量优先卖家精灵，仅 SIF-only 词回退 SIF；原始来源值、周期、规范来源和回退标记并存。SPR/商品数量只取卖家精灵，SIF-only 保持空值。
5. 自动清洗使用跨类目动态中心对象框架：目标完整商品及本类目已验证等价表达进入 Sheet2；相邻直接替代完整商品进入 Sheet4；配件/零件、多商品、Gift、第三方品牌/IP、泛词、残缺词、无关词和缺流量数据进入 Sheet3。正式运行不逐词中断；不能可靠识别时保守进入 Sheet3并写具体原因。
6. 首次输入记录自有品牌及别名。自有品牌目标完整商品词可进入 Sheet2并加`自有品牌词`；其他品牌和未声明授权 IP进入 Sheet3。英文字母拼写错误保留原词并加`错字错词`，不增加纠正字段；其他语言目标完整商品进入 Sheet2并加`其他语言`。同义词、有效词形/词序、缩写和其他语言不等于拼写错误。类目内已验证等价表达只作用于当前类目，不能变成跨类目固定词典。
7. F5 先保留全部多标签，再按固定标签优先级选择一个`长尾主分组标签`；每个标签组内按 ABA 从高到低排序，超过 20 个词再拆 `LT-1`、`LT-2`……。每个关键词只出现一次，每个子组最多 20 个。
8. SIF 竞争正式 CPC、Top3 点击/转化份额、市场 CVR和入场信号取机会筛选的完整关键词精确匹配值；竞争格局接口只作辅助。无精确匹配不使用近似词替代。比较池回退至全部 F1–F4仍少于 10 时，不输出综合竞争等级，统一`数据不足/人工复核`；旧运行中样本不足仍输出`中`的结果不再有效。
9. 卖家精灵趋势返回的月份必须非空且可解析；空月份响应无效，调整获准参数重查，不自行生成月份。
10. 工作簿构建固定加入 CSV先导入临时空工作簿、可读日期、长主键/来源/周期/说明列宽与换行、全 Sheet 渲染、趋势固定图表区域预览，以及关键词/月份/图表序列闭环检查。
11. Sheet3 否词正式边界继续通过后续真实验证迭代。本次没有新增可直接执行的精准或词组否定规则；广告资格、广告模板和按广告目的填词仍不处理。

## 2026-08-20：单一职责 Skill 拆分与独立质量验证

用户明确要求在第一次正式只读验证前完成长期副任务对应的 Skill 结构。该决定只调整执行所有权和包结构，不改变 V2.1 清洗基线、V2.2 词频组件、竞争阈值、趋势来源或任何业务去向：

1. 旧 `amazon-keyword-source-collection` 组合包拆为 `amazon-keyword-sif-competitor-collection`、`amazon-keyword-amazon-autocomplete` 和 `amazon-keyword-sellersprite-expansion`；三个来源各自独立回传，三来源机械合并和第一板块十表总门继续由主任务 operations 拥有。
2. 旧 `amazon-keyword-competition-trend-analysis` 组合包拆为 `amazon-keyword-competition-analysis` 和 `amazon-keyword-trend-analysis`；竞争保持 Sheet2 F1–F4、机会筛选精确值和样本不足10阻断等级，趋势保持 Sheet2 F1–F3与卖家精灵月度搜索量唯一正式来源。
3. 新增 `amazon-keyword-quality-validation`，只读检查 Run/revision、来源、主键、人口、版本、公式、渲染、Sheet和图表闭环；它不修复上游、不调用业务外部系统、不新增业务判断。
4. 当前项目共十二个 Skill，全部为`draft`，能力为`planned`。本次拆分和 P0 通过不产生 P1 证据；旧 gaming-chair 运行不能反向成为新包证据。

该结构变更不命名为新的业务版本，也不替代2026-08-20首轮正常案例规则回写；它只为独立仓库首次正式只读验证建立单一职责执行边界。

## 2026-08-20：并行调度依赖确认

用户核对迁移前后总流程后明确确认两组执行关系：

1. SIF完成并由主任务确认主锚点与1–3个代表种子后，Amazon联想和卖家精灵扩词可以并行；主任务三来源机械合并必须等待两个分支及SIF全部回传。
2. Sheet2/3/4清洗闭环后，词频统计和关键词分类可以并行。词频不参与分类判断；最终工作簿装配仍必须等待词频分支和分类后竞争/趋势分支全部完成。
3. 关键词分类完成后，竞争性分析和趋势性分析继续按既有任务架构并行，最终装配在汇合门之后开始。

该确认只消除流程图与任务架构之间的顺序歧义，不修改V2.1清洗基线、V2.2词频规则、任何阈值、来源、去向或输出人口，也不产生P1证据。

## 2026-08-21：首轮真实运行后的字段与竞争减配

本次根据首轮真实运行的用量、接口返回和工作簿装配结果，减少不再参与业务判断的返回字段与跨任务上下文。它不改变 V2.1 清洗基线、V2.2 词频组件、Amazon 联想必选门或趋势规则；趋势内容留待后续单独调整。已确认：

1. 关键词竞争性仍只覆盖 Sheet2 F1–F4，但正式判断只使用 SIF 的`Top3点击份额`与`Top3转化份额`。2026-08-13 的 CPC、SPR、商品数量、市场 CVR、入场信号、机会筛选、竞争格局、比较池和样本不足10门全部从当前竞争合同移除。
2. Top3 两项继续沿用已确认绝对阈值：`<30%`低、`30%–<50%`中、`50%–<70%`高、`>=70%`极高；两项合成为头部锁定度并直接作为当前`综合竞争等级`。两项相差至少15个百分点时继续记录结构分化。由于不再计算相对分位数，竞争输出不再需要10词样本门。
3. 竞争分析优先复用锁定第一板块 SIF 竞品反查表中的完整词 Top3 两项；同词多竞品记录一致时合并来源，不平均。缺失或冲突时，只允许使用 SIF `market_get_keyword_history`按完整词批量查询，并只采用`latest`中的日期、Top3点击份额和Top3转化份额；近似词、词根和历史均值不得替代。
4. 卖家精灵 `keyword_miner` 固定传`returnFields=keyword,keywordCn,searchRank,searches`。副任务本地保存接口实际返回的原始响应和分页证据，机械融合后装配一张仅含`英文关键词、中文翻译、ABA月排名、月搜索量`四列的来源表；主任务只接收工作簿、相对路径、哈希、行数、分页/缺失/冲突状态和异常摘要，不接收逐页业务数据正文。
5. 卖家精灵最大召回不因字段减配而缩减：每个锁定种子至少执行两个版本化完整 Pass；第二个 Pass 新增或交换机械键时最多追加第三个 Pass。声明总数/页数漂移只作证据，只要页码持续前进、返回可用、无整页重复/循环并能到达短页或空页结束即可继续。持续错误只阻断受影响 Pass，不得抹去已取得结果或阻止其他锁定种子执行。
6. SIF `market_get_asin_keyword_signals`当前不支持接口侧字段选择，因此每次完整工具响应仍先落本机原始证据。副任务随后只装配`竞品ASIN、SIF返回序号、英文关键词、ABA排名、搜索量、Top3点击份额、Top3转化份额`七列明细和核心词候选摘要；300条接口上限本次不变。主任务不读取原始长响应正文。
7. 第一板块和最终工作簿不再要求 SPR、商品数量或其他已移除竞争字段。`04_SIF原始数据`和`06_卖家精灵原始数据`保留原 Sheet 名以维持十表兼容，但只承载当前最小字段子集；完整接口响应、分页证据和来源事件映射继续留在 Run 本机目录与清单中。
8. 所有相关 Skills 继续保持`draft`、能力保持`planned`。本次字段和合同减配不是 P1 证据；首轮旧工作簿不按新规则改写，必须从锁定输入重跑后才能验证新合同。

## 2026-08-21：输出合同、趋势、通用词库和独立QA最终确认

用户随后逐项确认过程文件减负、最终使用工作簿、趋势计算和质量验证结构。本决定替代此前“第一板块十表”“最终总表只收Sheet2”“全过程Sheet全部塞入最终工作簿”“词频保留介词”“趋势只画月搜索量”“Sheet3输出否定方式”和“品类通用词库待定”的口径：

1. 正式Run只交付两个顶层对象：`过程性文件/`和一个七Sheet最终工作簿。过程目录保存完整证据和过程表；最终工作簿只保留最终决策总表、SKU事实卡、品类产品通用词库、竞争、趋势、词频和否词库。
2. 第一板块业务工作簿从十表减为`Sheet1_关键词池`和`Sheet2_任务信息与类目锚点`；所有三来源关键词进入机械词池，缺指标不删词。
3. 第二板块固定四Sheet且取消完整源表副本。Sheet2/3/4分别使用确认的13/13/12列合同，第二板块不打语义标签。
4. 分类只增强Sheet2和Sheet4；Sheet2追加四固定列和N个品类自适应语义列，Sheet4只追加四固定列。否词库固定五列，只保留原始完整词和语义原因，不判断精准/词组否定。
5. V2.2词频更新为过滤固定版本英语介词；介词从单词表删除并作为相邻双词断点，不跨介词连接。
6. 竞争输出固定十二列，只处理Sheet2 F1–F4并保留Top3完整明细；最终总表只回写竞争性强度和数据状态。
7. 趋势每个F1–F3精确词至少拉取24个完整月，展示最近12个月搜索量/环比/同比和最近4个完整季度搜索量/环比/同比，并生成月度与季度两张百分比折线图。
8. 最终总表人口改为Sheet2/3/4三去向全人口，固定50列加N个动态语义列；详细Top3及竞争结构是唯一明确不进入总表的原字段组。广告资格和投放动作暂写`未评估/后置`。
9. 品类产品通用词库只从最终总表中品类相关行机械筛选。每个流量层或语义分类使用`关键词、月搜索量、竞争性强度`三列块，多标签词可进入多个块。
10. `process-manifest.json`成为唯一总清单；最终装配执行21项门。独立QA只输出两Sheet质量工作簿、质量manifest、必要预览和同一问题文档，保持只读。
11. 本决定不改变V2.1清洗基线、不把V2.2升级为整个项目版本，也不产生P1。旧工作簿必须按新固定revision从头重跑。

## 2026-08-22：核心词确认与完整词覆盖优先级澄清

本次真实验证中，较宽泛但已被产品事实和三竞品SIF共同支持的品类核心词曾因主执行锚点更具体而被误判为歧义。用户确认这属于执行错误，并授权把优先级写回知识、判断边界、主任务和清洗Skill。本决定不新增三去向、不改变流量门，也不命名为新清洗版本：

1. 主任务必须同时锁定一个较具体的`主执行锚点`、全部`已确认核心词`和`已确认等价表达`。主执行锚点服务来源矩阵、代表种子和任务路由，不是唯一允许进入Sheet2的词。
2. 核心词和等价表达确认必须同时有产品完整商品事实和直接竞品SIF候选支持；SIF流量单独存在只能产生候选，不能确认类目。
3. 较宽泛词只要与本Run一级品类完整商品对象一致且证据闭合，就可成为已确认核心词。不得仅因词面较短、存在一般词典多义或主执行锚点更具体而降级为歧义。
4. 清洗在流量门后先执行精确锚点表达门：机械键与已确认核心词或已确认等价表达完全一致时进入Sheet2。其他词必须读取完整短语，决定性修饰语、商品头部和配件语法可以改变中心商品；疑似拼写变体不能绕过这一门。
5. 当完整词由锚点、已确认等价表达或其可靠的单复数、所有格、空格、符号噪声、短编辑或相邻换位变体，加上不会改变商品对象的属性、结构、材质、尺寸、功能、价格、配送、风格、受众、场景或配置词组成，且没有第二商品头部时，进入Sheet2。可靠变体必须不是另一有效商品词，且词形与完整上下文共同唯一指向本Run商品对象；无法识别的字符串不得因来源或词根自动进入Sheet2。人工复核不得作为宽泛核心词、普通属性词或可靠词形变体的批量兜底。
6. 核心词根不能自动放行所有短语，也不能导致所有含该词根的短语被批量排除。`本体 for 使用场景`与已经形成另一商品类别的`场景+核心词`必须按实际交付物分别判断。
7. 修改前生成的锚点卡或清洗去向只作旧规则问题证据。必须从最早受影响模块按更新后revision重跑并保存差异，方可继续验收；本次规则写回和P0检查不产生P1。

## 2026-08-22：词频介词白名单固定

词频真实任务在输入闭环后因“固定英语介词表”没有精确内容与版本而正确停止。用户随后确认固定白名单并授权先写回词频Skill、稳定知识和同步流程，再恢复原词频任务。本决定只消除V2.2词频组件的运行歧义，不修改V2.1清洗、不追溯改写旧输出，也不产生P1：

1. 当前唯一词频介词白名单命名为`EN_PREP_CORE_V1`，包含且只包含稳定知识与词频工作簿合同列明的48个唯一英语token；Run必须同时锁定版本、完整内容、token数量和内容SHA-256。
2. 标准化后精确命中的白名单token不进入单词词频，并作为相邻有序双词的硬断点；不得删除介词后跨两侧重新连接。
3. `a/an/the`、`and/or`、`up/down/off/out/as/like`不属于该版本白名单，必须保留。其他停用词也不因常见程度被额外删除。
4. 词频组件不做上下文词性重判，不解析多词介词，不调用外部词典动态增删白名单；多义token只按白名单成员身份机械处理。
5. 版本、48-token数量、内容或哈希任一不一致，以及运行时发现静默扩表或额外停用词过滤时，词频任务停止并保留证据。
6. 该确认属于V2.2组件合同补全。修改前的blocked记录应保留为停止门有效证据；恢复任务必须使用更新后的规则快照重新开始分词与计数。

## 2026-08-22：来源备用入口、完整词语法与趋势图表合同确认

用户确认以下内容属于跨品类、跨Run的通用执行合同，不记录当前测试案例事实。本决定补充来源访问入口和完整词语法，并替代此前“Amazon联想只能使用内置浏览器”“SIF/卖家精灵只能使用MCP”“趋势只用卖家精灵”和“趋势折线图展示环同比百分比”的口径：

1. Amazon联想仍是必选来源，固定环境仍为Amazon US未登录、类目All、邮编10001。首选Codex内置浏览器；当前设备无法稳定识别或操作时允许使用普通Chrome备用入口。两种入口执行同一输入矩阵和同级可见证据门；两者均不可用时才记`not_executed`。
2. 联想候选被轮播或横向控件遮挡时，输入保持不变，通过控件使其完整可见并保存操作前后证据。仍不能完整读到的候选只记异常，不进入来源事件；辅助技术返回的数量不能替代可见文本。
3. SIF和卖家精灵均首选各自MCP；MCP不可用时允许使用同一提供商的已登录网页入口。备用入口必须保持同一站点、查询对象、周期、分页深度、业务字段和完整性门，并记录`source_provider`、`entry_type`、查询时间、分页闭合与原始导出/可见证据。网页入口不得换提供商、换指标或补造字段。
4. 入口切换后，只有存在稳定来源事件标识、可去重且能证明无遗漏/无重复时才允许断点续采；否则从受影响ASIN或种子的完整查询/Pass重新开始。已取得的其他完整查询结果继续保留，不因入口切换被抹去。
5. 清洗中的配置连接词只提供语法信号，不直接决定去向；并列连接结构可能表达两个主要商品，不得自动按随本体配置放行。其他语言按其商品头部、修饰关系和词序判断中心购买对象，不套用英语词序。该规则必须对锁定人口整批执行，并反向抽查Sheet2误放与Sheet3误摘。
6. 趋势来源优先级固定为`SellerSprite -> Sorftime`，两者只查询完整精确词月搜索量。一个Run的全部F1–F3趋势人口只能使用一个锁定提供商；主来源中途失败时保留诊断证据，并从头用备用来源重跑全部人口，不混源、不跨源补值。主来源恢复只影响下一个新Run。
7. 月度折线图展示实际月搜索量，横轴为全部可用完整年月；季度折线图展示三个完整月之和，横轴为全部可用完整自然季度。每个关键词在每张图恰好一条线。月环比、月同比、季环比和季同比只留在矩阵表格，不进入图表序列。
8. 缺失月份继续留空，不填0、不插值、不前值延续；季度缺任一完整月时，该季度搜索量和比例留空且不进入季度图。以上合同同步不改变V2.1清洗基线或V2.2词频组件，不产生P1证据。

## 2026-08-23：核心层级、单一种子与通用词库资格修正

用户确认此前把一级品类、产品细分核心、强等价表达和宽泛/相邻流量词合并为“多个已确认核心词”的结构存在系统性风险；同时确认“品类相关”不自动等于“本产品强相关”。本决定替代2026-08-22“较宽泛候选证据闭合后可成为已确认核心词”及“全部品类相关行进入通用词库”的相关口径，不记录当前测试产品、关键词或ASIN：

1. 每个Run必须且只能锁定一个`一级品类核心大词`，并可锁定最多一个`产品细分核心词`。存在细分核心词时，`主执行锚点`必须等于该词；不存在时，主执行锚点必须等于一级品类核心大词。
2. `强等价表达`要求与主执行锚点双向替换后中心商品不变。上位品类、宽泛用途/场景和相邻细分词另记为`宽泛/相邻流量词`，不获得细分核心或强等价层级。
3. 核心证据优先级为产品中心对象与决定性细分特征、直接竞品同对象身份、SIF竞品覆盖/返回位置、ABA与搜索量。流量只在同一语义层级内验证和排序，不能跨层级晋级；营销用途、推荐场景或主题字段不能单独改变产品对象层级。
4. 卖家精灵每Run只使用一个代表种子：有已确认细分核心词时使用该词，否则使用一级品类核心大词。强等价表达和宽泛/相邻流量词不默认成为种子。
5. 第二板块三去向继续按V2.1一级品类边界执行；同一级品类其他细分可以留在Sheet2。为区分产品强相关人口，Sheet2新增`通用词库资格`，只用`纳入/不纳入/待复核`，固定字段由13列调整为14列。
6. 一级品类核心大词、细分核心词、强等价表达及目标细分同对象扩展词可写`纳入`；同一级品类其他细分、宽泛用途/场景流量词或不能唯一描述目标商品对象的词写`不纳入`；未解决`待复核`不进入产品强相关下游人口。
7. 分类完整传递资格且不重算。词频、竞争和趋势分别只处理资格为`纳入`的适用Sheet2人口；最终品类产品通用词库只从`最终去向=品类相关 AND 通用词库资格=纳入`机械筛选。
8. 最终总表由固定50列加N动态列调整为51列加N动态列，新增`通用词库资格`。最终七Sheet数量与顺序不变；第二板块`Sheet4_二类词`仍留过程工作簿，二类词全量进入最终总表并以`最终去向=二类词`识别。
9. 独立QA继续使用21项Gate，但Gate 2必须检查分层锚点、单一种子和资格人口，Gate 9检查资格筛选，Gate 10检查产品事实与锚点关系；QA还须对全部锚点/等价词/种子及SIF头部候选、通用词库高流量词做有界语义反向抽查。
10. 修改前使用混层锚点、多个代表种子、13列Sheet2、全部品类相关人口下游分析或旧通用词库筛选生成的产物只作问题证据。更新后必须从最早受影响的机械合并/锚点卡开始重跑；规则同步和P0验证不产生P1。

## 2026-08-24：最终工作簿新增二类词独立Sheet

用户确认“二类词”指第二板块`Sheet4_二类词`中的直接替代完整商品，不是F2“二级流量词”，并要求最终工作簿增加独立可见Sheet。该决定只调整最终装配与QA合同，不改变V2.1三去向、二类词语义门、流量门、分类规则或任何分析适用人口：

1. 最终工作簿由七个可见Sheet调整为八个，顺序固定为`最终关键词决策总表、SKU事实卡、品类产品通用词库、二类词、关键词竞争性分析、关键词趋势性分析、词频统计、否词库`。
2. 新增最终`二类词`Sheet机械复制分类完成的`Sheet4_二类词`全人口，不从总表或原始词池重新判断；每个二类词`Keyword_ID`恰好一行，上游零行时保留表头并视为合法零人口。
3. 字段固定为第二板块十二列加分类四列，共十六列：`Keyword_ID、英文关键词、中文翻译、ABA月排名、月搜索量、关键词来源、流量数据来源、数据状态、中心购买对象、二类商品类型、共同核心购买任务、直接替代理由、流量层、长尾主分组标签、LT分组、分类状态`。
4. 最终装配不得新增、删除、重判二类词或改变上游值；该Sheet不得加入Top3、动态语义列、竞争、趋势、通用词库资格、否词方式、广告资格或投放动作。
5. 最终总表继续覆盖三去向51+N全人口，过程工作簿继续保留原Sheet4；新增最终Sheet只是面向使用的独立视图，不建立第四去向或第二套二类词人口。
6. 二类词仍只参加既有F1–F5流量分层和F5主分组/LT；仍不进入品类产品通用词库、词频、竞争或趋势，也不自动成为否词或广告结论。
7. 两个顶层交付对象和21项装配门总数不变。Gate 6增加二类词Sheet人口/十六列/主键/值闭环，Gate 16和Gate 18分别改验八Sheet顺序和八Sheet渲染。
8. 该合同同步不产生P1证据。修改前的七Sheet工作簿只保留为旧合同运行证据；必须按八Sheet合同重新装配并通过独立QA后，才可代表当前交付合同。

## 2026-08-24：输入核心门、卖家精灵网页导出优先与任务分离

用户确认以下三项是跨品类、跨Run的通用规则，不记录本次测试产品、关键词、ASIN、账号或本机任务事实。本决定细化2026-08-23核心层级，替代2026-08-22“卖家精灵MCP优先”的入口顺序，并强化既有长期副任务所有权：

1. 用户在Run开头只回答目标Amazon类目，以及该类目是否存在多个稳定产品类型细分；用户不指定一级品类核心大词或产品细分核心词。
2. 主任务结合锁定产品事实卡、获准直接竞品同对象身份和SIF流量候选，独立确认唯一一级品类核心大词。只有用户确认类目存在多个稳定产品类型细分时，主任务才可判断最多一个产品细分核心词；用户确认不存在时该字段必须留空。
3. 单独卖点、配置、配件、材质、尺寸、功能、容量、使用场景或高流量长词不能自行建立产品细分核心层级。竞品覆盖、返回位置、ABA和搜索量只在同一语义层级内验证与排序。
4. 卖家精灵单一种子规则不变：有细分核心词时使用该词，否则使用一级品类核心大词。关键词挖掘入口改为拥有长期副任务内置浏览器中的已登录卖家精灵网页端优先；网页存在完整官方导出时必须使用，网页不可用时才使用同提供商MCP。
5. 每次完整官方导出是一个版本化Pass。至少完成两个Pass，第二Pass有机械键新增或交换时最多追加第三Pass；保存导出任务身份、时间、页面声明总数和原始文件，并用实际行数、唯一机械键和四个业务字段闭合。界面位置只是操作提示，不是稳定规则。
6. 只有官方导出不可用且逐页采集仍能证明无遗漏/无重复时才允许逐页备用；否则状态为`partial/blocked`。网页登录会话和凭据只属于当前设备，每Run验证，绝不进入仓库、manifest或交付物。
7. 来源、清洗、词频、分类、竞争、趋势、最终装配和独立QA必须由各自长期单一职责副任务执行并正式回传当前Run相对路径、哈希、人口、状态、缺口和验证。主任务只负责输入/核心层级锁定、调度、正式回传验收、机械合并、阶段门、冲突和交付。
8. 副任务不可用、未初始化、操作不便或主任务已能看到证据都不是代跑理由。只有用户了解影响并明确批准的一次性Run例外，主任务才可接收已经产生的证据；例外必须留痕，不能成为默认路由、长期授权或P1能力证据。
9. 本次合同同步不改变V2.1三去向、通用词库资格、下游人口、八Sheet结构或21项门数量，也不产生P1。使用旧输入门、旧卖家精灵入口优先级或主任务代跑的受影响Run只能按实际状态作为边界/问题证据。

## 2026-08-24：Amazon联想一级品类输入与可采集区域澄清

用户确认以下边界为跨品类、跨Run的Amazon联想通用合同，不记录当前测试产品或页面个案：

1. Amazon联想固定矩阵只使用本Run唯一一级品类核心大词。`主执行锚点`在存在细分核心词时会等于细分核心词，因此不得作为本模块输入字段或替代一级品类核心大词。
2. 可采集区域只限搜索框上方关键词联想区。普通关键词建议行、关键词建议卡片和关键词建议组内选项只有在文本完整可见时才可形成来源事件。
3. 底部商品卡、价格卡和商品轮播不属于关键词联想来源；即使文本和价格完整可见也必须排除，不能形成来源事件。
4. 只有关键词联想区内部的关键词建议被轮播或横向控件遮挡时，才在输入不变的前提下操作该区域控件并保存前后证据；不得为了采集底部商品/价格内容操作商品轮播。
5. 本澄清不改变固定环境、输入矩阵、浏览器入口优先级、三来源合并、V2.1三去向或P1状态，只消除输入层级和可见区域歧义。

## 2026-08-24：第二板块两级歧义分流

用户确认“完整词仍无法消歧”必须按判断层级拆分，不得同时允许Sheet2和Sheet3两种去向：

1. 若完整词仍无法确认中心购买对象是否属于一级品类，则进入`Sheet3_其他摘除`，摘除主类型按`泛词/残缺/歧义`记录，并标记人工复核。
2. 若中心购买对象已经确认属于一级品类，只是仍无法判断其与目标细分的关系，则保留在`Sheet2_品类相关`，写`品类关系=关系待复核、通用词库资格=待复核`。
3. Sheet2待复核行在问题解决前不得进入词频、竞争、趋势或最终品类产品通用词库；Sheet3歧义行也不自动成为否词。
4. 三去向仍要求每个`Keyword_ID`恰好一个去向。机械人口闭环不能替代这项两级语义抽查，质量门必须分别检查Sheet2误放和Sheet3误摘。
5. 本修正不改变V2.1三去向定义、Sheet字段数量、流量OR门、二类词门或下游适用人口，只消除Sheet2/Sheet3待复核边界冲突，不产生P1。

## 2026-08-24：分类数据缺口状态、最终QA确认与通用词库显示名

用户确认分类中的ABA/搜索量缺口是行级数据状态，不应作为分类阶段整批人工停止门；同时确认最终`品类产品通用词库`五个流量块的面向使用显示名。本决定不改变ABA阈值、F1–F5内部代码、分类人口、三去向或下游资格范围：

1. ABA排名未抓取到或不可用时，分类状态精确写`关键词ABA排名缺失`；流量层、主分组和LT留空，不用搜索量反推。
2. 月搜索量未抓取到或为空时写`搜索量缺失`；来源明确返回无搜索量或数值为0时写`没有搜索量`。搜索量缺口不覆盖有效ABA分层。
3. 三种行级数据状态不提前停止分类整批。受影响派生值不伪造；受影响主键、原始值、状态和留空字段写入manifest及唯一问题文档，并传递到最终装配。
4. 最终工作簿候选装配完成后，独立QA核对清单并由主任务向用户确认。确认未闭合前QA为`incomplete`而不是`blocked`；用户接受当前数据状态后，可按合同允许缺口评估`pass/completed_with_gaps`。若用户要求改值，回到分类拥有副任务修订，再重新装配和QA；质量任务仍保持只读。
5. `品类产品通用词库`上半区五个流量块显示表头依次为`F1 核心大词、F2 二级词、F3 中流量词、F4 中长尾词、F5 长尾词`。这只是最终工作簿表头变更；分类过程和最终总表仍使用F1–F5内部代码。
6. 本次不新增或改写F5唯一主标签选择优先级。Sheet4在精简动态语义列后的F5主标签取值依据仍为开放问题，不在本决定中擅自补规则。

本决定替代历史文本中把ABA缺失统一写为`ABA未提供`或将这类行级缺口作为分类整批人工停止门的口径。不产生新清洗版本，不产生P1证据。

## 2026-08-25：来源双种子、同类目二类词、配置假阴性与封包审计修正

用户在最近一次真实验证后逐项确认以下跨品类规则。该Run的历史来源和工作簿只保留为问题证据；用户明确要求本次只修复稳定合同和检查能力，不重跑任何关键词数据、不修改历史工作簿。F5唯一主标签跨Run优先级继续暂缓：

1. 来源锚点由核心层级机械决定。存在已确认细分核心词时，Amazon联想使用细分核心词；卖家精灵分别使用一级品类核心大词和细分核心词。不存在细分核心词时，两者只使用一级核心词。强等价、宽泛流量、卖点、配置或场景词不得成为额外种子。
2. 卖家精灵关键词挖掘以拥有长期任务中的已登录官网完整官方导出为首选；使用关键词挖掘页当前可用的顶部/左上导出入口并从导出记录取得完成文件。官网不可用时才使用同提供商MCP。各种子原始XLSX和Pass分开保留，模块内按机械关键词键生成一个四列合并交接表；重复词仍保留全部seed/Pass来源身份。
3. 用户确认类目存在多个稳定产品类型细分时，Sheet4不再要求中心对象离开一级品类。同一级品类内，决定性完整语义已形成与目标不同的稳定产品类型，且解决相同主要任务、无需额外主要商品并通常同次比较时，可以成为二类词。普通属性、卖点、材质、结构、尺寸、功能、人群或配置不能自行建立二类产品类型。
4. 第二板块不得把“显式包含细分核心词/强等价表达”或“匹配目标SKU配置”当成通用词库`纳入`硬门。SKU事实只用于中心对象和决定性稳定类型消歧；目标SKU没有某材质或配置，不能把同品类完整词降级、摘除或转二类。一级核心完整商品加普通非变物修饰、没有决定性其他类型时，仍按目标细分同对象扩展评估`纳入`。
5. 清洗与独立QA都必须从Sheet2`不纳入`、Sheet3和Sheet4反向检查假阴性，覆盖一级核心商品加属性/材质/结构/功能/配置及未显式包含细分核心词的同对象表达；人口、字段和格式闭环不能替代语义反查。
6. 正式派发增加机械任务身份门：逻辑角色、固定标题、Task ID/host、执行cwd、Run、revision和输出目录必须同时匹配本机忽略映射和当前任务状态。任何字段不符时停止，不由主任务代跑，也不创建临时通用子代理。实际身份值仍不得进入tracked文件或交付物。
7. 趋势包级审计必须遍历全部worksheet、drawing和chart关系，分别确认月度/季度图，并证明图表只引用实际搜索量序列。业务声明有公式/图表而审计得到0时，先判定审计器自身失败，不得把工具误报写成业务失败或静默通过。
8. 隐私扫描分别覆盖文本、路径、XLSX单元格和OOXML结构；真实Codex任务ID及含任务ID的本机路径必须失败，普通SHA-256和Office内部GUID必须通过。进入交付的模块路径统一改写为稳定相对路径，并由装配与独立QA交叉扫描。
9. 最终封包采用候选装配、独立QA一次性不可变最小产物、装配后最终重哈希、QA只读封包差异核对四阶段。最终process manifest不计算自身；质量目录使用允许清单，未列文件零容忍；最后一次QA核对不再写文件，避免循环失效。
10. 当前有效最终合同继续是八个可见Sheet。历史七Sheet记录保留并明确为被替代的历史证据；当前规则或检查器不得把历史文本误判为有效七Sheet合同。

本次修正仍属于Post-V2.1增量，不建立新的清洗版本，不提升任何Skill成熟度，也不产生P1。

## 2026-08-26：多细分类目的目标细分简称强等价闭环

用户确认：只有目标Amazon类目存在多个稳定产品类型细分且已确认产品细分核心词时，才需要额外区分目标细分、同类目其他细分和宽泛流量；该分支不能演变成“关键词必须逐字包含细分核心词或一级品类词”的字面硬门。本决定不记录当前测试产品、关键词或ASIN，不改变V2.1三去向、流量门、来源种子或八Sheet结构：

1. 多细分类目在来源下游调度前必须完成`目标细分强等价闭环`。主任务逐项检查省略一级品类词、用途限定词或其他上位限定词的完整商品候选，确认其是否仍保留决定性细分表达和完整商品头部。
2. 强等价结论继续按既有证据优先级建立：产品事实中的中心商品与决定性细分特征、直接竞品同对象身份、SIF覆盖/返回位置；ABA和搜索量只在同一语义层级内排序。共享词根或高流量不能单独晋级。
3. 省略上位限定词后仍唯一交付目标细分商品的稳定市场简称或紧凑表达，锁定为`强等价表达`；第二板块写`品类关系=强等价同对象、通用词库资格=纳入`。其普通非变物扩展按目标细分同对象扩展继续判断。
4. 缺少一级品类词、用途词或预设同义token本身是中性词面事实，不能单独成为宽泛/相邻、Sheet3摘除、`通用词库资格=不纳入`或Sheet4二类词依据。省略后可稳定指向多个细分、另一商品或不完整对象时，才按完整词保持宽泛/相邻、未确认或其他适用去向。
5. 强等价、宽泛/相邻和未确认候选必须逐项记录结论、依据和风险；同一机械键不得跨层级重复。用户默认只确认目标类目及是否存在多个稳定产品类型细分，核心层级不得伪标为`用户确认`，除非用户确实逐项确认。
6. 清洗发现锁定宽泛/相邻候选与完整词、产品事实、直接竞品身份和SIF组合证据共同指向目标细分时，属于上游锚点冲突。清洗停止受影响输出并回传主任务，不自行升级，也不沿错误锚点继续分流。
7. 清洗与独立QA必须专门反向抽查多细分类目的上位限定词省略族，并扫描全部分流理由。任何仅以缺少一级品类/用途token为理由的批量分流均失败；共享模板理由只定义待审人口，不得替代逐行结论或把整组自动宣告为假阴性。
8. 当前受影响的真实Run获准在本批稳定规则与Skills同步、仓库校验完成后，从最早受影响的锚点锁定/第一板块交接开始生成新版本产物，并重跑全部依赖输出和独立QA。旧工作簿与冻结QA继续作为问题证据，不得就地改写或冒充P1。

本次仍是Post-V2.1合同增量，不命名为新清洗版本；规则同步和P0校验本身不产生P1。

## 2026-08-27：十二Skill证据登记与真实三案例接纳边界

用户要求十二个Skill全部建立`evidence/index.md`，并从全新正常案例01开始依次完成两个正常案例和一个边界/异常案例。该决定只补充P0/P1治理，不改变任何来源、阈值、语义判断、输出人口或工作簿合同：

1. 每个Skill从`draft`阶段起固定登记`case-01-normal.md`、`case-02-normal.md`和`case-03-edge-or-error.md`三个槽位；索引只表示结构与状态，不是案例证据。
2. 当前十二个Skill共36个槽位全部从`planned`开始。此前真实Run、历史工作簿、模板、AI推演和事后补写不得倒填或追认为本轮案例。
3. 正常案例必须在同一锁定Git revision上由拥有模块的长期副任务真实完成核心步骤并通过自身质量门；`blocked`、`partial`、`not_executed`或一次性主任务代跑不能占用正常槽位。
4. 边界/异常案例必须真实触发并正确执行预期的安全停止、同提供商备用、缺口保留或人工升级；整体Run可以被阻塞，但对应Skill的边界行为仍须独立验收。
5. 一个端到端Run可以为实际执行到的每个Skill各贡献一个对应案例，但每个Skill必须形成独立、脱敏、引用自身capability ID且可审计的模块记录，不能用整轮结论替代模块证据。
6. 正式只读运行期间不修改Skills、知识、流程或证据索引。Run结束并经用户确认后，才在获准迭代/发布批次写入脱敏案例文件并更新索引；三槽位全部`accepted`且被引用能力均有真实验证依据后，才可评估`maturity: verified`。
7. 本次登记结构和P0验证不产生P1，不改变V2.1/V2.2或Post-V2.1业务规则。正常案例01必须使用包含本批结构与规则的全新锁定revision和全新输入。

## 决策边界

- V0.1–V2.0 只用于追溯规则形成、修订和案例校准，不作为当前执行基线；各行状态说明其被哪个后续版本替代或吸收。
- V2.0 已被 V2.1 替代，不与 V2.1 并列作为当前基线。
- 当前唯一项目清洗基线是 V2.1，但“当前版本”不代表第三板块或整个知识库已经完成；V2.2 是词频组件业务基线，不与 V2.1 竞争清洗规则归属。
- 2026-08-11至2026-08-24的第三板块、竞争、趋势、核心词和最终装配决策只在未被后续决定替代的范围内继续有效。2026-08-23的单一种子与2026-08-24的一级核心词联想规则已由2026-08-25条件式双种子/细分联想锚点替代；同日修正同类目二类词、配置假阴性、任务身份、趋势审计、隐私和QA后封包。2026-08-26进一步补充多细分类目的目标细分简称强等价闭环、层级来源和冲突停止门。当前权威结构是唯一一级核心、可选唯一细分核心、条件式强等价闭环、条件式联想锚点和一至两个卖家精灵种子、两对象交付、两Sheet第一板块、四Sheet第二板块、三去向51+N总表、分类Sheet4机械复制的最终二类词Sheet、八Sheet最终工作簿、资格纳入人口的词频/Top3-only竞争/24月趋势，以及固定48-token介词过滤。以上均不改变V2.1清洗基线，也不代表能力已通过P1。
- 后续业务规则变更必须记录来源、适用范围、复核日期和替代关系；结构整理本身不产生新业务版本。

## 开放问题

来源字段与入口优先级、条件式Amazon联想锚点与底部商品/价格卡及商品轮播排除、用户类目细分输入门、主任务核心判断、多细分类目的目标细分简称强等价闭环、机械固定任务身份、分层核心、卖家精灵条件式双种子和官网完整导出优先、第二板块14列/同类目二类词/配置安全资格、分类传递、资格纳入人口的`EN_PREP_CORE_V1`介词过滤词频/Top3竞争/单Run单源24月趋势、实际搜索量折线图与包级审计、否词库、三去向51+N总表、通用词库、分类Sheet4机械复制的最终二类词Sheet、八Sheet两对象交付、隐私扫描和QA后封包均已确认为当前合同。当前开放项是F5唯一主标签跨Run优先级、广告否定方式、广告资格和投放动作，以及按当前固定合同完成新的正常案例P1；所有Skills仍需真实P1证据才能标为`verified`。

## 相关资料

- 稳定领域知识：`product-keyword-library.md`
- 历史案例证据：`keyword-cleaning-case-evidence.md`
- 当前项目状态：`../PROJECT.md`
- 当前判定边界：`../docs/keyword-judgment-boundaries.md`
- 历史 V0.1 草案：`keyword-library-method-v0.1.md`
