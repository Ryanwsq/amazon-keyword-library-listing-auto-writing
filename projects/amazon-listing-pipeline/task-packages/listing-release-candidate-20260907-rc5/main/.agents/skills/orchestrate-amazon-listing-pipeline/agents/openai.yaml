interface:
  display_name: "Amazon Listing 自动化总控"
  short_description: "串联上游证据、七类人工校准、埋词与 Listing 交付"
  default_prompt: "Use $orchestrate-amazon-listing-pipeline to lock the input marketplace, obtain authenticated receipts for every required Amazon/SIF/SellerSprite owner session, then coordinate the seven-part calibration preview before final Listing assembly."
