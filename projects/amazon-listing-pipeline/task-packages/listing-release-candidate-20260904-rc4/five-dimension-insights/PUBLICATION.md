# 发布候选：SANITIZED_CANDIDATE_NOT_PUBLISHED

本目录是用于机械合入的发布候选，尚未提交、推送或发布，也不代表任何角色已实际装载。只验证候选结构、来源、适配与授权范围；未启动业务、未验证新真实案例、未提升P1。

目标仓内路径为 `projects/amazon-listing-pipeline`。当前rc4以rc3为不可变前序；rc2保留德国站路由、Rufus和来源优先级修复，rc3追加输入模板安全字段与固定八Task/host会话登录合同，rc4补齐必填marketplace与八会话初始化回归。原项目维护源、历史版本和业务数据保持原位；原聊天、实际任务绑定、原始输入输出和采集日志不进入候选。

## 单一资产的明确公开例外

标签Skill的黄金资产 `golden-tag-priority-workbook.xlsx` 原件含真实ASIN、Alexa采集内容、评分与结论。用户在获知会公开这些内容后明确允许原件公开。授权标记为 `specific-user-approved-public-example`，只覆盖SHA-256为 `7ee355536f9ca806b82b1d91d08d8bec0f56f9ba90df99068a05707d8a28cb9c` 的该文件及角色包内同字节必要副本。

该文件原样保留五张Sheet、1452个公式、版式与缓存，没有清空、替换、改公式或造案例。其他原始业务资料、聊天、任务ID、账户、凭据及设备路径不因本例外获准发布。样例不提供新SKU事实，公开许可不是新的业务验收或P1。

## 已更新的输入模板

总控输入模板只包含字段、下拉和说明，没有真实商品记录。rc2将必填`市场选择`放在`新品基础信息`最后一列，不再保留`运行配置`Sheet或`keyword_skill`输入覆盖；rc3新增`登录准备`Sheet，只允许SIF/卖家精灵的非敏感账户别名、凭据引用与本机登录方式，Amazon固定为用户手动登录，严禁密码、验证码、Cookie或令牌字段。语言由站点派生，填写完成后总控立即按固定八Task/host会话矩阵引导登录。未设置德国站专属后台限制时保持待确认，不套用美国站默认值。模板变化须与输入合同、校验脚本和角色包哈希一致。

## 规则与证据

公开规则中的匿名说明只解释判断条件，不提供新SKU事实，不是新训练数据或accepted案例。原始讨论记录只在本地保留，候选提供完整现行规则正文与逐项沿革导航。未确认待办没有生效。输入事实、未知、样本不足、技术失败、取消QA及P1原状态不得升级。

逐文件清单见项目根 `release/candidate-manifest.json`；来源与候选适配见 `release/source-provenance.json`。不将原项目的旧校验或角色装载回执转记为本候选验收。角色包的完整来源信息同时保存在各自package-manifest.json；实际运行仍需安装环境明确绑定和输入授权。
