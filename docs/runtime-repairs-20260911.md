# 本次验证后的执行修复

下表是rc7修复验收时点记录，保持原修复范围。随后用户授权SIF竞品反查改为`SIF MCP > SIF官网`，只对新锁定流程生效；当前顺序见[统一来源路由](workflow-input-output-and-source-routing.md)，不按本历史表的MCP备用条件启动新Run。

维护批次：`RUNTIME-REPAIRS-20260911-01`。只记录脱敏根因和维护落点；真实输入、任务ID、Run文件和来源问答留在Git忽略目录。旧交付和旧角色包不覆盖。

## 问题与修复落点

| 问题 | 修复 | 仍需区分的边界 |
|---|---|---|
| 测试入口擅自禁止历史复用 | Listing派发请求明确`keyword_entry`；默认复用优先，只有可定位用户指令可选fresh-only；关键词拥有任务执行原30天资格判断 | 同站点、同类目、条件式同细分、原始最终输出时间和完整性门不变；新电脑不会自动取得私有词库 |
| 登录完成后没有立即启动、漏并行分支 | 全图扫描保持强制，登录门完成即处理所有就绪动作；真实发送包装可解析，失败/错误目标拒收 | 扫描器不自动控制Codex工具，主任务仍必须实际发送并留回执 |
| app cwd被误用为业务执行根 | 关键词派发预检分别核对app cwd、Git根与显式业务根，确认目标脚本/规则存在 | 目录存在不等于已接收或已采集 |
| 恢复后读到旧产品Run | 关键词checkpoint重新验证活动dispatch、Run、版本、输入/规则及允许读写路径；同dispatch续跑 | 未修复应用压缩或模型容量，只防恢复动作继续串Run；历史消息不是新授权 |
| SIF网页登录成功但导出失败 | 同提供商MCP备用新增完整导出失败触发，核验失败证据、当前授权、鉴权及原查询锁 | 不是换提供商；未登录不直接降级，完整成功ASIN不重采 |
| 卖家精灵周期漏锁/误用完整月份 | 明确最近30天；接收派发前校验周期、种子、过滤和字段锁 | 来源指标名称/阈值/人口不变；不把周期未知伪装已对齐 |
| 来源自报完成但实际证据缺失、页面与导出人数不同 | 联想逐格验证持久化证据；导出核验真实行数与声明，差额未解释保持partial | 不把平台总数、导出上限或75/75自报当完整证据 |
| 手动登录被输入校验误拒绝 | SIF/卖家精灵模板和校验增加用户手动，凭据引用可空；已保存凭据模式仍需引用 | 八个Task/host会话证明不减少；Amazon只由用户手动登录 |
| 已自动填充登录表单又反复等用户 | 允许一次登录点击并复核当前身份/查询锁 | 禁读、复制、补填或记录秘密；验证码、失败/第二次跳转仍交用户 |
| 确认正文后装配被缺哈希阻断 | `confirm-copy`同时登记阶段output_sha256，确认人记录不伪装用户审阅 | 双确认门不改；不能凭“继续写”批准正文 |
| 五点被反复压短、已有软性规则未落实 | 每条实际正文绑定购买理由/事实机制/动作情境/收益/事实位置及质量评语 | 不新增字符下限/目标/上限；脚本不能证明自然性，语义仍由写作拥有任务判断 |
| 最终表固定列丢失，渲染后才返工 | 单一布局JSON提供既有字段骨架，先校验基础矩阵/表，再搬源表；最终接受再次检查14Sheet/文件名 | 只固化布局，源表保真、内容/公式/图表与视觉检查仍保留，不恢复独立QA |
| 未采用标签进入T排名、低样本显示正式百分比 | 在装配合同重申07确认与09实际采用人口闭合；未采用另区、无资格频率留空、无人工重要度不排序 | 是原业务边界的执行修复，不改上游P级/权重/样本阈值 |
| 最终回传后又改行高，主任务拿到旧版 | 自建表先处理布局，正式渲染/保真结束才一次回传最终09/10与回执；改动另存候选，重新绑定哈希 | 不覆盖已确认09计划或旧最终；主任务仅收到完整封存回执后镜像 |

## 验证及部署

合成回归覆盖正反例、原门保留、旧版本兼容；根统一检查入口为`python3 -B scripts/validate_projects.py`。逐项实际结果见Listing发布目录`release/runtime-repairs-review.json`。角色新版本为`listing-release-candidate-20260911-rc7`，注册表指向新快照，不修改rc1–rc6。

本批不重新采集、上传Amazon、执行真实端到端或P1；浏览器下载故障、应用压缩和模型服务容量不声称已修复。真实速度和人工干预次数仍需下一次获准运行验证。代码存在、新包生成、任务已完整重载、GitHub已推送是不同状态；本批完成时逐项报告，不沿用旧回执。
