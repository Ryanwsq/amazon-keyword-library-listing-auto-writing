# Amazon workflow repository routing

本仓库容纳两个独立项目；根目录不拥有采集、清洗、分类、SKU终筛或Listing写作规则，不执行或越权代跑业务模块。

新电脑只把总仓添加为一个 Codex 项目，在其中保留两套独立业务角色。整条产品输入到 Listing 交付的统一入口是 Listing `main`；关键词 `keyword-main` 继续拥有关键词子流程及其固定副任务。统一入口只改变编排归属，不授予主任务代跑权限；用户明确指定关键词单项目任务时仍使用关键词入口，不新增 Listing 依赖。

先按用户任务选择项目，再完整读取该项目的AGENTS及其规定的资料：

- 关键词词库：`projects/amazon-keyword-library/AGENTS.md`。
- Listing撰写信息决策：`projects/amazon-listing-pipeline/AGENTS.md`。执行具体角色前使用该项目当前角色包注册表与明确派发的包路径。
- 跨项目交接：分别完整读取两方的交接合同，保留各自Run/输入/事实/版本/哈希、正式回执及Listing主任务READY权限。合仓不允许项目主任务代跑副任务。

项目文档中的项目根、相对路径和本机产物目录均以所属项目根为基准；Git revision则是本总仓的提交。不要在总仓根直接运行模块业务脚本，也不从其他项目或全局同名Skill回退。根目录不再平铺业务Skill发现入口。

安装先核对目标 Codex 项目及既有任务，只有用户明确授权创建时才补齐缺少的注册角色；同名但属于旧项目的任务不计入新目标，旧项目、旧任务及历史文件不迁移或删除。关键词角色表和 Listing 当前角色包注册表分别决定职责与完整加载顺序，两接收整理角色不得遗漏。记录实际 app cwd、显式业务执行根、Git 根及角色包版本/哈希，不能把本机映射当作权限、真实装载回执或业务 READY。详见 `docs/cross-device-setup.md`。

来源/阈值/人口、全部判断边界、人工门、质量路由和P1状态由原项目拥有文件决定；迁移检查不是业务运行、装载或P1。生产运行前须重新核对本机目标任务、cwd及版本，不沿用旧绝对路径或历史Run。任务ID、凭据、真实工作簿及原始证据仅保存在Git忽略的本机目录。

仓库维护先读取`docs/merge-and-publication.md`。统一机械检查入口为`python3 -B scripts/validate_projects.py`，它不代替任何原项目检查或真实业务验收。不得因合仓删除必要模板/规则、弱化检查、跨项目覆盖事实，或改变已冻结历史产物。
